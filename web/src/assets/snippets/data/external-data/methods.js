//Uppercasemodel.Employee.entityMethods.nameCaps = function () {    var result = this.first_name.toUpperCase() + ' ' + this.last_name.toUpperCase();    return result;};//To add a datastore class method that returns the first X entities, passed as parameter:model.Employee.methods.getFirstOnes = function(limit) {limit = limit || 2000;    var col = ds.Employee.query("id < :1", limit);    return col;};